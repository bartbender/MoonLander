{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "firefox",
            "request": "launch",
            "name": "Launch Moon Lander in Firefox",
            "url": "${workspaceFolder}\\index.html",
            "webRoot": "${workspaceFolder}",
        }
    ]
}
